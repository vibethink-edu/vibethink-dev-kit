# DECISION REGISTER — auditable ledger of authority approvals (the dev-kit itself)

> **Append-only.** Every decision of an authority class (a "go ahead", an approval
> to proceed, a seal) is registered here the **moment it is granted** — especially
> approvals given through ephemeral channels (chat) that leave no other durable
> trace. This is the dev-kit's own instance of
> [`CANON-STATE-MIRROR-AND-DECISION-REGISTER-001`](../../knowledge/methodology/CANON-STATE-MIRROR-AND-DECISION-REGISTER-001.md)
> §6 (the kit dogfooding its own canon). An ADR answers *why this design*; a row
> here answers *who authorized acting, and how we know.*

**Authority classes recognized here** (per [`CANON-CHANGE-PATH-AND-DECISION-CLASSES-001`](../../knowledge/methodology/CANON-CHANGE-PATH-AND-DECISION-CLASSES-001.md) §4): **canon/law changes** (a new or amended spine, a piece registered in `setup/ADOPT-DEV-KIT.md`) are **authority-sealed** by the Principal Architect — never delegated. Tooling version-forwards and doc tweaks are **delegated-with-record** (listed here for one timeline; their *why* lives in the PR/ADR).
**Channel of record:** chat approval, confirmed by the merge of the cited PR.
**Timezone of the *when* field:** UTC.

| # | When (UTC) | Decision (what) | Proposed by | Authority (granted by) | Channel | Evidence |
|---|---|---|---|---|---|---|
| D-001 | 2026-06-15 14:19 | **SEAL** of the governance-instruments + coder-orchestration elevation — registers ADOPT pieces **#34** (state-mirror-and-decision-register), **#35** (coder-safe-identity), **#36** (coder-orchestration); statuses flipped to SEALED | dev-kit architect (from a vertical's elevation handoff) | Principal Architect | chat ("dale aprobado") | [PR #83](https://github.com/vibethink-edu/vibethink-dev-kit/pull/83) MERGED → master `cc65046` |
| D-002 | 2026-06-15 14:42 | **SEAL** of **#37** change-path-and-decision-classes — closes the gate-decision gap for heirs (path: direct/spec-first/design-gate; classes: authority-sealed/delegated/autonomous) | dev-kit architect | Principal Architect | chat ("si dale") | [PR #84](https://github.com/vibethink-edu/vibethink-dev-kit/pull/84) MERGED → master `cba9dc2` |
| D-003 | 2026-06-15 | **Version-forward** graphify pin `0.7.13 → 0.8.39` (delegated-with-record; version-forward policy = PR + real-machine evidence) | dev-kit architect | delegated (under "ajusta los pendientes") | chat | exercised on `vm-XL-app-cup` (Python 3.12.8): `--version` → 0.8.39 + `update` → 141 nodes/233 edges; this PR (supersedes [PR #75](https://github.com/vibethink-edu/vibethink-dev-kit/pull/75)) |
| D-004 | 2026-06-15 | **SEAL** of **#38** db-security-baseline (Postgres/Supabase exposed-schema security floor) — engine-specific by direction (`N-A(non-postgres)`); carries the `PUBLIC`-grant silent-no-op discovery + self-testing-migration discipline + Supabase advisor as CI/pre-cutover gate. Same seal: cross-repo-handoff discovery rule added to `CANON-MULTI-AGENT-ORCHESTRATION` §2 | dev-kit architect (from ViTo W3–W7 advisor convergence) | Principal Architect | chat ("Sella el canon de los descubrimientos … asegúrate de que se aplique para Postgres") | this PR |
| D-006 | 2026-06-16 | **SEAL** of the **coder permissive-deny permissions template** ([#114](https://github.com/vibethink-edu/vibethink-dev-kit/issues/114)): ships `setup/templates/coder-permissions/` (broad-allow Bash + an airtight `deny` list — identity / destruction / secrets / cloud-apply / arbitrary-exec hatches) + wires it into `RUNBOOK-LAUNCH-CODERS.md` §5. L3 instantiation of `CANON-CODER-ORCHESTRATION-001` §4/§7 + `CANON-CODER-SAFE-IDENTITY-001` §8 (no canon change). NOT a CI gate (expectation/tooling, §10). Deny-list reviewed line-by-line + approved by the architect; fila C narrowed from a broad `*secret*` glob to a concrete file list to avoid recon false-denies | dev-kit architect (from the Campus coder-marathon learning) | Principal Architect | chat ("aprobado como propones") | this PR |
| D-005 | 2026-06-16 | **SEAL** of the **versioning instrument + gate** (answers a consumer's elevate-back, [#103](https://github.com/vibethink-edu/vibethink-dev-kit/pull/103) part 1): ships `setup/templates/versioning/` (live version source + binding) + the `check-versioning` gate, and **amends `CANON-VERSIONING-001` §5** to bless the `YYYY.MM.DD+<shortSha>` build-metadata form for stateless/deploy-in-commit apps. Closes "policy without mechanism" for app/package versioning. Parts 2–3 (UAT version display, presentation-debug) routed to the consumer — out of scope for the markdown+CLI supra | dev-kit architect (consumer elevate-back, G-008) | Principal Architect | chat ("aprobado para dev-kit") | [PR #118](https://github.com/vibethink-edu/vibethink-dev-kit/pull/118) MERGED |
| D-008 | 2026-06-16 | **SEAL of the feature-documentation instrument + gate** (closes the §5/§6 "no shape, no gate" gap surfaced auditing tri-repo inheritance): ships `setup/templates/feature-docs/` (requirements · readiness-plan-with-security · roadmap · append-only log · per-unit changelog · `FINDING` + a README discoverability map that points at each governing canon **including the versioning rules**) + the `check-feature-docs` gate (config-driven, skip-when-no-config, wired into `devkit-doctor`), and **amends `CANON-DEVELOPMENT-PROCESS` §5** to point at the instrument. Filenames stay L3. Same closure pattern as the §5 versioning instrument (D-005). | dev-kit architect (from the tri-repo inheritance audit) | Principal Architect (delegated merge) | chat ("A dale" / "mergealos vos") | [PR #134](https://github.com/vibethink-edu/vibethink-dev-kit/pull/134) MERGED → `b71c5fa`. Companion: omnigent prior-art + runtime-policy-engine ADR, ACCEPTED via [PR #136](https://github.com/vibethink-edu/vibethink-dev-kit/pull/136) → `5cd285f` — see D-009. |
| D-011 | 2026-06-17 | **SEAL of 2 wire-to-cloud process findings** (consumer comms, from Campus's wire-to-cloud): (a) `CANON-TESTING-MINIMUM-BAR-001` **§6.2** — env-portable integration self-tests (the self-test provisions its own fixtures → "verified alive" means alive against the *deployed* env, not the dev seed; sibling of §6.1); (b) `REVIEW-READINESS-PROTOCOL` **§8.1** — cloud-first for review (the code-vs-review decision rule) + deployed = source-of-truth / local = mirror (lifts a consumer migration-testing binding to the supra layer). Both **fold-in** (no new spine), agnostic (fire-test). Both comms → `actioned`. | dev-kit architect (evaluating the consumer findings) | Principal Architect (delegated merge) | chat ("GO") | [PR #142](https://github.com/vibethink-edu/vibethink-dev-kit/pull/142) MERGED → `622fb21`; both markers → SEALED |
| D-010 | 2026-06-16 | **SEAL of `CANON-RUNTIME-POLICY-ENGINE-001`** (the spine the ADR/D-009 called for): runtime governance by policy interception — ALLOW/ASK/DENY at typed enforcement points, composition + precedence (DENY short-circuits · ASK withholds writes · ALLOW continues), session state, fail-closed, the pattern menu, and §11 worked examples for the L3 implementer. L1 contract only; the engine is L3. Runtime sibling of `CANON-CODER-ORCHESTRATION-001` (#36). Registered as ADOPT **piece #40**; `catalog-sync` exemption removed. Agnostic (fire-test) · extract-patterns-not-dependency (no omnigent dep). | dev-kit architect (from the omnigent prior-art) | Principal Architect (delegated merge) | chat ("wow dale") | [PR #139](https://github.com/vibethink-edu/vibethink-dev-kit/pull/139) MERGED → `47cb2a0`; status → SEALED |
| D-009 | 2026-06-16 | **ACCEPT of `ADR-20260616-runtime-policy-engine`** — canonize the runtime policy-interception pattern (from the omnigent prior-art, source-verified) as a **NEW agnostic spine** `CANON-RUNTIME-POLICY-ENGINE` (over folding into `CANON-CODER-ORCHESTRATION-001` — different altitude). L1 = the contract (ALLOW/ASK/DENY · enforcement points · precedence · fail-closed · ASK-withholds-writes); the concrete engine stays L3 (each product builds it on its own pain). Extract-patterns-not-dependency (no omnigent dependency). **Next step (not yet done):** draft the spine `DRAFT → PROPOSED → seal`, each its own gate. | dev-kit architect (tri-repo prior-art research) | Principal Architect (delegated merge) | chat ("A dale" / "mergealos vos") | [PR #136](https://github.com/vibethink-edu/vibethink-dev-kit/pull/136) MERGED → `5cd285f`; ADR status → ACCEPTED |
| D-012 | 2026-06-17 | **SEAL of the identifier-language convention + the gate-coverage principle** (from a consumer's identifier-language gate finding — Campus gate green on schema while Spanish route slugs went unscanned): (a) `CANON-NAMING-CONVENTIONS-001` **§8** — new convention "Identifier language" (identifiers in one declared language; data/UI language stays in values & strings) **with the surface-complete clause** (the rule spans schema · route/URL slugs · file/dir names · config/env keys; one-surface enforcement is a false green) + table row + never-item + per-repo binding (declare the language **and** the surface set); sections 8/9/10 renumbered to 9/10/11; (b) `CANON-AUDIT-PROTOCOL` **§8.6** — the cross-cutting "declare your coverage surface; un-scanned ≠ green; partial-gate worse than missing-gate" principle that unifies `CANON-TESTING-MINIMUM-BAR-001` §6.1/§6.2 + NAMING §8. Both **fold-in** (no new spine), agnostic (fire-test). The concrete Campus fix (gate-extension to `app/` + slug rename ES→EN) is the **L3 follow-up** for the Campus seat. FINDING comm → `actioned`. | dev-kit architect (evaluating the consumer finding) | Principal Architect | chat ("go") | [PR #144](https://github.com/vibethink-edu/vibethink-dev-kit/pull/144); both markers → SEALED |
| D-007 | 2026-06-16 | **Adoption-tooling suite + the consumer-found fix** (delegated-with-record — tools/docs, no canon; the day's timeline): `devkit-doctor --adoption` inventory lens ([#124](https://github.com/vibethink-edu/vibethink-dev-kit/pull/124)); `devkit-upgrade` one-shot — pull + re-sync copied runnables + provision-missing-tools + surfaced per-tool messages ([#126](https://github.com/vibethink-edu/vibethink-dev-kit/pull/126)/[#127](https://github.com/vibethink-edu/vibethink-dev-kit/pull/127)/[#128](https://github.com/vibethink-edu/vibethink-dev-kit/pull/128)); `devkit-doctor` `doc/` status-path fix from the WorkBench seat's finding while applying #129 ([#130](https://github.com/vibethink-edu/vibethink-dev-kit/pull/130)); developer command cheat-sheet in `USING-THE-KIT` §1.6 ([#125](https://github.com/vibethink-edu/vibethink-dev-kit/pull/125)); two governed comms — WorkBench apply ([#129](https://github.com/vibethink-edu/vibethink-dev-kit/pull/129)) + Rodrigo macOS upgrade guide ([#131](https://github.com/vibethink-edu/vibethink-dev-kit/pull/131)); [#103](https://github.com/vibethink-edu/vibethink-dev-kit/pull/103) closed by disposition (part 1 = D-005/#118; parts 2–3 = consumer/UI). Also the engine-test coverage of all `.mjs` (#115/#116) + Self-Check content gate (#117) earlier the same day. All autonomous/delegated class. | dev-kit architect | delegated (acknowledged by Marcelo) | chat ("sella") | the PRs cited (all MERGED) |
| D-013 | 2026-06-17 | **SEAL of `CANON-PORT-ASSIGNMENT-001` + the ports instrument** (from the agnosticism audit, hallazgo #2): the kit mandates **THAT** an instance declares its ports canonically, **never WHICH**; no canonical declaration → **deploy refused** (fail-closed). Ships the canon (L1) + recommended template `setup/templates/ports/` + the `check-ports` gate (config-driven, skip-when-no-config, fail-closed; 10/10 tests; wired into `devkit-doctor`, kit dogfoods deploys:false). L1/L2 split: `knowledge/PORT_ASSIGNMENT_GLOBAL.md` refactored to the **L2-house binding** (concrete VibeThink ranges only) pointing at the canon — the agnostic core no longer knows the numbers. Registered as ADOPT **piece #41**; `catalog-sync` exemption removed; SUPRA-MAP regenerated. a/b decision: the concrete map **stays as L2-house in the kit** (cross-product; relocating to one product would couple the others). Agnostic (fire-test). | dev-kit architect (agnosticism audit) | Principal Architect | chat ("GO" + "sella") | [PR #145](https://github.com/vibethink-edu/vibethink-dev-kit/pull/145); status → SEALED |
| D-014 | 2026-06-17 | **SEAL of `REVIEW-READINESS-PROTOCOL` §8.1.1** — *pure visual iteration runs local* (sharpens §8.1/D-011): adds the decision gate *"does this review need real data or a deploy-only path? **No** → local (visual/UX, zero CI/CD spend); **Yes** → deployed/cloud"*. §8.1 had folded ALL UAT into cloud; pure visual tweaks (color/layout/props/copy) need no real data and burn CI/CD per tweak → local. **Ports kept orthogonal** (the gate decides the *layer*, never the *port*; `CANON-PORT-ASSIGNMENT-001` applies independently when local) — no coupling, no explicit see-also (Marcelo: minimal/implicit). Minimal amend, no scope creep. From a ViTo-architect handoff; agnostic (fire-test). | dev-kit architect (from the ViTo handoff) | Principal Architect | chat ("GO al contenido") | this PR; handoff → actioned |
| D-015 | 2026-06-18 | **SEAL of the identifier-language gate elevation** — the **mechanism** for the already-sealed `CANON-NAMING-CONVENTIONS-001` §8 (rule) + §8.6 (surface-complete), which had no gate ("policy without mechanism"). Kit-elevation of a consumer's proven CI engine (Campus `check-schema-identifier-language`): ships `tools/check-identifier-language.mjs` (agnostic — **vocabulary-allowlist, not freeze-at-N denylist**; surface-complete: schema DDL · route/URL slugs · feature dirs) + `.test.mjs` (8/8) + `setup/templates/identifier-language/` + config example; wired into `devkit-doctor` (skip-when-no-config) + CI + `versions.json`. Registered as ADOPT **piece #42**. **Fire-test refactor on lift:** the consumer engine hardcoded product layout (`apps/web/src/app`, `apps/web/src/features`) + Postgres column types + schema dirs → ALL moved to the per-repo binding; the engine names no product/language/layout (surfaces + vocabulary stay L3). Inheritance: Campus consumes the kit engine via copy-parity; retiring the old `check-d6-slugs.mjs` is the Campus seat's hygiene. | dev-kit architect (kit-elevation from the Campus D-6 consolidation finding) | Principal Architect | chat ("SELLA") | [PR #150](https://github.com/vibethink-edu/vibethink-dev-kit/pull/150); ADOPT piece #42 |
| D-016 | 2026-06-18 | **SEAL of `CANON-DATA-CHANGE-AUDIT-001`** (new agnostic spine, from a consumer directive): every system must answer **who/what/when** of an operational data change, **full history**, via a **generic change-capture audit-trail** — **not** per-table `updated_by`/`updated_at` columns (level-0, last-touch only). Names the level-0 trap, the required generic shape `{table,op,id,old,new,who,when}`, the **orthogonality** clause (row timestamps `created_at`/`updated_at` are a different concern and stay — they are not the trail, and the trail is not built from per-table columns; resolves the apparent conflict with the per-table-timestamp convention), scope (operational tables; exclude static config / read-only legacy / high-frequency telemetry), "who" = real authenticated identity, trail is governance-only. Mechanism is L3 (trigger+audit table · event log · CDC · temporal). Registered as ADOPT **piece #43**. Reference impl = Campus (Postgres/Supabase `audit.record_version`); agnostic (fire-test — engines are illustration only). | dev-kit architect (lift from the consumer directive) | Principal Architect | chat ("Otra Sellá") | this PR; ADOPT piece #43; proposal → actioned |
| D-017 | 2026-06-18 | **SEAL of `CANON-DATA-CHANGE-AUDIT-001` §9 (retention)** + **OPEN of `CANON-DATA-LEGAL-COMPLIANCE-001` (DRAFT)** — from `FINDING-LEGAL-COMPLIANCE-DOMAIN-UNGOVERNED`. (a) **§9 retention (SEALED):** the audit-trail is partitioned + kept hot for a window + **archived (cold), never hard-deleted**; the window is a **legal requirement** bound to the new legal canon §2 (L3 number, not the kit's); true deletion only on a legally-required data-subject erasure (governed/logged exception); scheduler is L3. (b) **legal-compliance canon OPENED as DRAFT** (Marcelo "abre", not seal): agnostic mechanics — retention-by-type · recorded-withdrawable-consent · consented-tracking · data-subject-rights · declared-jurisdiction; the laws/jurisdictions/numbers are L3 (GDPR/Habeas Data/LGPD = illustration). **Not sealed**: pending the consent-canon separation review (action 3) + the jurisdiction/retention-number decisions; `catalog-sync` exemption added (→ adoptable piece on seal). | dev-kit architect (from the consumer finding) | Principal Architect | chat ("abre … amend …") | this PR; §9 → SEALED; legal canon → DRAFT (exempted); finding → actioned (action 3 = ViTo review pending) |
| D-018 | 2026-06-18 | **SEAL of `CANON-DATA-LEGAL-COMPLIANCE-001`** (DRAFT → SEALED). Action 3 (consent-canon separation review) **done** — the relay was sent to the consumer architect (ViTo `CANON-DATA-CAPTURE-CONSENT-001` refactors on its next re-seal to point here for the agnostic mechanics: data-subject rights + jurisdiction timelines, portability, transparency, erasure-vs-retention; keeps its L3 numbers + bindings; capture philosophy stays L3). The other "open item" (jurisdiction/retention numbers) is **L3 by design** — the canon never holds a number — so it was never a seal blocker. Seals the agnostic mechanics; registered as ADOPT **piece #44**; `catalog-sync` exemption removed. Agnostic (fire-test — regimes are illustration only). | dev-kit architect (from the consumer finding + the action-3 review) | Principal Architect | chat ("enviado, y sella") | this PR; status → SEALED; ADOPT piece #44 |
| D-019 | 2026-06-18 | **SEAL of `CANON-DOCUMENTATION-ARTIFACT-STANDARDS-001`** (PROPOSED → SEALED; WorkBench-architect handoff, Marcelo principle "bind to a recognized standard, don't invent"). One standard per doc artifact, each bound to its SOTA (MADR · RFC/RFD · Google SRE PRR/postmortem/runbook · Keep a Changelog · SemVer+Conventional Commits · Diátaxis · C4/arc42). Supersede: **PRDI→PRR**, **ADR→MADR**, adopt Diátaxis. **Open decision resolved by Marcelo ("quitarlo"): the work-journal (`BITACORA`/`LOG`) is REMOVED** (folded into Changelog+ADR+git). Ships 7 templates (`adr/rfc/prr/postmortem/runbook/diataxis/architecture`). Registered as ADOPT **piece #45**; catalog-sync exemption removed. **Kit reconciliation in the same seal:** removed `feature-docs/LOG.template.md`, fixed its README + config example (dropped BITACORA/PRDI), and noted the removal in `CANON-DEVELOPMENT-PROCESS` §5. Consumer re-bind (WB drops LOG; ViTo updates its feature-lifecycle rule) = their lane, after seal. Agnostic (fire-test — binds public external standards only). | dev-kit architect (WorkBench-architect handoff) | Principal Architect | chat ("quitarlo" + seal) | PR #154 (PROPOSED) + this PR (seal); status → SEALED; ADOPT piece #45 |
| D-020 | 2026-06-18 | **AMENDMENT to `CANON-DATA-CHANGE-AUDIT-001` §10 (what to audit / what not, and why)** — folds two ViTo-architect triage handoffs. **§10.1 (SECURITY):** the generic old/new-capturing trigger **NEVER** runs on a secret store / sensitive-value table (it would replicate the secret inside the log — breaks minimal-secret-surface); options (a) exclude or (b) metadata-only (no old/new). **§10.2:** the triage matrix — AUDIT operational/PII/official/financial/consent; DO-NOT-audit secrets/keys · credential-bearing connections · logs/queues/events/metering/telemetry · static config/catalogs · jobs/derived/media/caches · backups/migration-ledger/system, each with the why. Concrete table names stay L3 (explicit reviewed list, never auto-include — a real triage had an auto-classifier wrongly mark a `*_api_keys` table to audit). Both handoffs → actioned. Campus-review suggestion relayed. | dev-kit architect (two ViTo-architect handoffs) | Principal Architect | chat (forwarded handoffs, "Aprobación: Marcelo") | this PR; §10 → SEALED |
| D-021 | 2026-06-18 | **REINFORCEMENT to `CANON-BRANCH-WORKTREE-LIFECYCLE` §5.2** (thin, low-prio; from a ViTo finding). Makes explicit that a per-task worktree is **id-unique AND fresh-on-reuse**: a name/dir colliding with a leftover (uncleaned per §5.4) is **removed + recreated fresh from the current base**, never reused stale — a reused stale worktree serves stale inputs and blocks the worker (evidence: a `037b` launch collided with a leftover `037`, served stale inputs, coder blocked). **Redirect noted:** the finding suggested `CANON-CODER-ORCHESTRATION` §9/§11, but that canon **explicitly disclaims** worktree ownership (§11) — worktree lifecycle is this spine's. The incident was already a §5.2 (no-reuse) + §5.4 (cleanup) violation; this makes the *fresh* word + the stale-input failure mode salient. | dev-kit architect (from a ViTo finding) | Principal Architect | chat ("decidí … low-prio") | this PR; §5.2 reinforced |
| D-022 | 2026-06-18 | **AMENDMENT to `CANON-CODER-SAFE-IDENTITY-001` §3 (+ §10) — the bot lock's enforcement is host-dependent** (first-heir macOS finding, Rodrigo). The §3 "lock" (protected default branch, PR+review) is a *hard* lock only where the **host enforces** branch protection; on some hosted plans/visibilities it is unavailable or **gated behind a paid org tier** (e.g. branch-protection rulesets on a *private* repo are not enforced without a paid org plan — the host warns it "won't be enforced"). Where the host can't enforce it, the lock degrades to **discipline, not enforcement**; §10 L3 binding MUST declare the fallback (public repo · alternative enforcement · discipline-only `N-A(reason)`). The identity asymmetry (bot = propose-only) still holds as a rule. Agnostic (the host is an example); non-blocking platform note, no change to the identity model. | dev-kit architect (forwarded first-heir finding) | Principal Architect | chat (forwarded, "forwardable") | this PR; §3 + §10 amended |
| D-023 | 2026-06-18 | **REFINEMENT of D-022 §3 note (`CANON-CODER-SAFE-IDENTITY-001`) — the fallback is itself host-dependent** (Marcelo's verified correction). The permission-based fallback (bot has no merge rights on the target — e.g. a fork model) only holds where the host's permission model allows that shape; concretely, on a major host a **read-only collaborator exists only under an org account, not a personal one** → the fork hard-lock needs an **org (a free org suffices), not a personal repo**. §3 now says the fallback must be one the host actually supports + "confirm it before relying on it". Prevents the next heir repeating the trap (assuming the fork model works on a personal free-private repo). Agnostic (host = example). | dev-kit architect (Marcelo's verified correction) | Principal Architect | chat (verified correction) | this PR; §3 note refined |
| D-024 | 2026-06-18 | **`devkit-upgrade` improvements** (delegated-with-record — tooling, no canon; from a WorkBench consumer's run): (1) **canon-delta report** — after the ff-pull, list the governance changes over the pull range (new DECISION-REGISTER D-rows + changed CANON/RUNBOOK/ADOPT files, excl. generated SUPRA-MAP) so the consumer knows *what to re-read*, not just "fast-forwarded N" (the consumer had acted on stale knowledge); (2) **template-instantiation surfacing** — list the kit's by-instantiation templates + flag config-backed ones not instantiated (the upgrade was mute on them); (3) **bug fix** — the pin hint printed `graphifyy` (double-y) → `graphify` in `external-tools.lock.json`. Test 6/6 (added template-gap case). #4 (fold adoption-check) left as the existing `--adoption` pointer (no heavy subprocess). | dev-kit architect (WorkBench consumer advisor) | delegated | chat (advisor request) | this PR; devkit-doctor 7/7 |
| D-025 | 2026-06-18 | **Narrow the coder-permissions `gh auth` deny** (delegated-with-record; WorkBench dogfood finding) — the template's `Bash(gh auth:*)` deny was too broad: it blocked the **read-only `gh auth status`** that a coder's PASO-0 identity check needs (a coder stopped at PASO 0 — correct halt, wrong reason). Narrowed to the **identity-change** subcommands only: `Bash(gh auth login:*)` + `Bash(gh auth switch:*)` + `Bash(gh auth logout:*)`; `gh auth status` now allowed. Matches the documented intent (`RUNBOOK §5` / README L23: veto identity-change, not the status read) and the template's own "tight & precise, not noisy" stance. Security preserved (no identity change); README table updated. Same narrowing pattern as D-006 (filaC `*secret*`→concrete). Bright side: the deny **bit for real** → the no-bypass deny-posture works. | dev-kit architect (WorkBench dogfood advisor) | delegated (Marcelo's forwarded recommendation) | chat (advisor + recommended narrow) | this PR; settings + README narrowed |
| D-026 | 2026-06-18 | **FOLD-IN: `CANON-TESTING-MINIMUM-BAR-001` §6.3 (exercise the consumer, not only the unit)** + sibling line in `CANON-AUDIT-PROTOCOL` §8.6 (from a ViTo finding — a PR merged broken with CI 100% green because CI built only the isolated unit, not the consumer; bundler tolerance ≠ type-checker tolerance; a declared export pointed at a never-committed file). §6.3: CI exercises **≥1 real consumer** of a changed unit (the layer that gives the true signal) + **declared exports resolve to committed files**; a unit-only green is a false green about what ships. It is §8.6 applied to CI; the consumer build **is** the check (per-repo binding). **No new gate** (over-engineering guard, Marcelo): the consumer build already catches the export-resolve case — a bespoke `check-exports-resolve` would be a redundant partial proxy, dropped. Fold-in (no new spine), agnostic, passes the acceleration test (kills a class of broken-merges-on-green). Evidence: ViTo #3494/#3515/#3524. | dev-kit architect (ViTo finding) | Principal Architect | chat ("GO si estás seguro, no over-engineering") | this PR; §6.3 + §8.6 SEALED |
| D-027 | 2026-06-18 | **FOLD-IN: `CANON-AUDIT-PROTOCOL` §8.7 (a gate is not "ready" until it fails on a known-bad input and blocks)** — gate-integrity corollary, sibling of §8.6 but orthogonal (§8.6 = *what surface* a gate scans; §8.7 = *the gate actually fails when it should + actually blocks*). A gate is inert in two ways — never goes red when it should, or goes red but nothing is blocked — both read as protection while protecting nothing. Rule: not "ready" until (a) demonstrated to FAIL on a known-bad fixture (falsifiability — "test the test"/mutation-style) AND (b) required/blocking; an unproven or non-blocking gate is false assurance (the §8 root failure, produced at install time). The gate is itself a shipping unit → itself subject to §6.3. **Precedent (ViTo correction):** the gate ViTo added to stop the #3494 pattern itself **merged broken** (`typecheck` vs `type-check` typo → never ran the real step) **and did not block** a PR because not marked required → the pattern slipped through anyway. Bound to standard ("build affected dependents" + required status checks). Fold-in (no new spine, spine grep'd first), agnostic, passes the acceleration test. **Also this turn:** §6.3 clarified local-capable (verification may be a pre-push hook / coder verify / manual build — no hosted runner required; PR #164) + reciprocal "consumer over unit" cross-ref added to `REVIEW-READINESS-PROTOCOL` §3. **Correction noted (no canon change):** ViTo's L3 binding is ALREADY implemented (`turbo --filter='...[origin/main]'`, #3523/#3532) — not a pending follow-up; the reference impl stays L3, not cited in the agnostic kit. Evidence: ViTo #3494/#3523/#3531/#3532/#3524. | dev-kit architect (ViTo correction + new lesson) | Principal Architect | chat ("GO") | this PR; §8.7 SEALED + REVIEW-READINESS cross-ref |
| D-028 | 2026-06-18 | **INSTRUMENT: `check-gate-integrity.mjs` — the gate that audits the gates** (gives §8.7 teeth; Marcelo: the prose-only framing "está flojo"). A framing paragraph for a *load-bearing* principle is itself the false-assurance §8.7 condemns — so §8.7 gets a mechanism, not prose. The gate enforces the **portable half §8.7(a)**: every declared gate (`tools/check-*.mjs`) must ship a paired `.test.mjs` with a **known-bad case** proving it goes RED; no test, or happy-path-only, is REFUSED. Config-driven (`tools/gate-integrity.config.json`), skip-when-no-config, verdict-first, pure-Node zero-dep, wired into `devkit-doctor` (now 8/8). **Dogfood:** it is itself a `check-*.mjs` so it audits itself; run over the kit → GREEN, 13/13 gates already prove they fail (the discipline existed by convention; this makes it enforced). Known-bad detector matches the real assertion idioms (non-zero exit / failing verdict / non-empty error set / negative-path label); a future test that misses is **normalised, never allowlisted** (§8.1). Template `setup/templates/gate-integrity/`. The non-portable **§8.7(b)** (required/blocking) stays L3 (per-repo branch protection — a portable gate cannot read it). Registered in `versions.json` (`check-gate-integrity.mjs` 1.0; `devkit-doctor` →1.3). Test 7/7. | dev-kit architect | Principal Architect | chat ("creo que eso está flojo" → strengthen → "GO") | this PR; instrument SEALED, devkit-doctor 8/8 |
| D-029 | 2026-06-19 | **INSTRUMENT SET: coder-dispatch handoff** (closes a consumer's gap — Campus finding — with 2 live facets). The decision gate (`CHANGE-PATH-AND-DECISION-CLASSES-001` §3 + `VT-METHOD` step 4) decided *methodology* but never routed to the bot-coder dispatch spine (`CODER-ORCHESTRATION-001` + `CODER-SAFE-IDENTITY-001` §9 + `RUNBOOK-LAUNCH-CODERS`) — the jump was **tribal knowledge**; repos without an architect who knew it didn't use coders or improvised them (losing the guardrails: identity-gate, deny-guard-under-bypass, boundary design-gate). 4 agnostic faces: **(a) routing [F1]** — new `CHANGE-PATH` §3.1 "the gate's third output: executor + dispatch" (human → normal flow; autonomous coder → dispatched via the coder spine, never improvised) + a readiness precondition split §8.7-style (portable artifacts self-verifiable / live forge state L3) + a pointer in `VT-METHOD` step 4. **(b) prompt template** — `setup/templates/coder-prompt/` names the implicit CONTEXT/MODEL/PIECES/STEP-0-IDENTITY/READ-IN-ORDER/HARD-RULES/DESIGN-GATE/DO-NOT structure. **(c) ADOPT piece #46** "Launch your first coder" (discoverability on-ramp). **(d) readiness check [F2]** — `check-coder-launch-readiness.mjs` + config + template, wired to `devkit-doctor`: verifies the **portable** launch-surface artifacts (launch script / per-session settings / bot-token env-var declared / optional prompt dir) so an agent **self-verifies readiness instead of punting infra to the human** (who was becoming the per-repo verifier); the **live forge state** (bot low-priv, branch protected) stays L3 (§8.7b honesty). Its own known-bad test → `gate-integrity` now 14/14. **Live evidence:** a competent agent in another governed repo built the per-spec prompt right *by memory* but closed "didn't verify this repo's launch-surface, another lane" → punted to the human (F1 no-routing + F2 no-readiness). Agnostic (fire-test: no vendor/forge/product). `versions.json`: `check-coder-launch-readiness` 1.0, `devkit-doctor` →1.4. Tests: readiness 8/8. | dev-kit architect (Campus consumer finding) | Principal Architect | chat ("GO") | this PR; §3.1 SEALED + 3 instruments, devkit-doctor 8/8 |
| D-030 | 2026-06-19 | **AMEND `CHANGE-PATH-AND-DECISION-CLASSES-001` §3.1 — present the choice, don't resolve it silently (human-in-the-loop).** Refines D-029: §3.1 named the executor as the gate's third output but did not say how it reaches a human authority. Now: when a human is in the loop, the gate **presents path × methodology × executor as ONE short decision card with a recommendation (verdict-first) and waits for GO** — never silently resolves a genuine choice, never buries a short prompt in detail. **Fires only on a genuine fork:** trivial/reversible/no-contract → direct path → just implement, no card. This is the human-facing UX Marcelo asked for ("¿no puede parar y decirme: SpecKit / directo / vos / bot-coder?") — the legible face of the gate, so a **short prompt** triggers a **short menu**, not silent resolution nor over-explanation. One paragraph, agnostic (methodology set is L3), no new mechanism. Reconciles with the "default direct execution for short fixes" feedback: card on a fork, do-it-now on the trivial. | dev-kit architect | Principal Architect | chat ("GO") | this PR; §3.1 amended |
| D-031 | 2026-06-19 | **FOLD-IN: handoff slots named by role, not by the product that fills them** (consumer promotion proposal — ViTo). A coordination/closeout handoff step names the **role** (`auditor`/`reviewer`/`advisor`/`executor`), never the agent/product that fills it; **omit a slot with no handoff**; agent/vendor tokens are legitimate **only in symmetric traceability rosters** (the branch-author prefix). **Spine-coverage check (as asked):** `CANON-NAMING-CONVENTIONS-001` owns *identifiers* — a handoff slot is not an identifier, out of scope — but its §3 owns the **symmetric branch-author roster** (where a vendor/agent token IS legit); `CANON-MULTI-AGENT-ORCHESTRATION` owns the **handoff/router/closeout slot** → the normative home. Folded the rule into ORCHESTRATION §5.1 (sibling of *Routing of roles*), referencing NAMING §3; + a one-line "never" **pointer** in NAMING §9 (discoverability, not a duplicate). **No new spine.** Origin: ViTo's "Protocolo de cierre" step 4 was "MENSAJE PARA OPUS" (product = advisor by default) → corrected to "MENSAJE PARA {ROL}"; **ViTo = L3 binding** (its named closeout protocol is the adapter). Agnostic (fire-test: roles named, no vendor). | dev-kit architect (ViTo consumer proposal) | Principal Architect | chat ("te acepto como arquitecto tu análisis y recomendación") | this PR; ORCHESTRATION §5.1 + NAMING §9 SEALED |
| D-032 | 2026-06-19 | **DOC: kit-refresh trigger — a short prompt is enough** (delegated-with-record; on-ramp). `USING-THE-KIT` had the mechanical update recipe (§B + the `devkit-upgrade` one-shot) but **no human-language trigger** and no "present + wait" step — so a short operator prompt ("refrescá el dev-kit") didn't reliably map to the recipe (the gap a real operator question exposed). Added a **Refresh trigger** note: on any *refresh/update the kit* intent → (1) `devkit-upgrade` (pull + canon-delta), (2) re-read flagged canons, (3) present a **short adoption card** and **wait for GO** (`CHANGE-PATH` §3.1), (4) on GO wire + `devkit-doctor`. Intent→recipe mapping (agnostic; exact phrase is L3 in the consumer's root rules) so the **short prompt yields a short menu, not a silent mass-change**. No new mechanism; reuses `devkit-upgrade` + §3.1. | dev-kit architect | Principal Architect | chat ("damelo") | this PR; USING-THE-KIT updated |
| D-033 | 2026-06-19 | **ELEVATE the kit-refresh trigger to `AGENTS_UNIVERSAL` (inherit by reference, not paste).** Marcelo caught a real inconsistency: my D-032 advice told consumers to **paste** a trigger line into their `AGENTS.md` — which **violates the kit's own inherit-by-reference law** (docs inherit by pointer, never copy). Fix: added the trigger to `AGENTS_UNIVERSAL` Always-Do (the universal root authority every consumer already inherits), so Campus/ViTo wire **nothing** — they inherit the behavior. Kept the core **neutral** (abstract: upgrade / change-delta / health board — concrete tool names stay in the on-ramp / level 2; fire-test clean). USING-THE-KIT note updated to say the behavior inherits via AGENTS_UNIVERSAL; only the exact operator phrase is optionally L3. `AGENTS_UNIVERSAL` v1.3→v1.4. Refines D-032. | dev-kit architect | Principal Architect | chat ("¿no queda mejor en dev-kit? instrucción") | this PR; AGENTS_UNIVERSAL Always-Do + USING-THE-KIT |
| D-034 | 2026-06-19 | **FIX (tooling): `devkit-upgrade` was re-syncing `adapted` copies → silent revert of a sanctioned adaptation** (consumer finding — ViTo, severity medium). Repro: a copy marked `adapted` in `copy-parity.config.json` (which `devkit-doctor`/`check-copy-parity` correctly EXEMPT → GREEN) was still in `devkit-upgrade`'s re-sync set → applying the upgrade overwrote the local adaptation. Root cause: the re-sync loop did not check `c.adapted`. **Fix:** mirror the gate — skip any `c.adapted` entry (preserve it, never compare, never re-sync), report it as `◆ adapted (preserved)`; the consumer drops the flag deliberately when it wants upstream back. Confirmed both **dry-run and apply** now skip (the finding's open question — the skip is before the DRY branch). `devkit-upgrade` 1.2→1.3; test 6/6→7/7 (new case covers dry-run + apply, asserts the local file is untouched). No canon change; delegated-with-record. | dev-kit architect (ViTo consumer finding) | delegated | comms finding (INVESTIGATE/FIX) | this PR; devkit-upgrade fixed |
| D-035 | 2026-06-19 | **FOLD-IN: multi-system orientation (GROUND-FIRST + the systems-map) → `CANON-AGENT-SCOPE-DISCIPLINE` §2.1** (consumer promotion — ViTo). **Spine-coverage check (as asked):** `GROUND-FIRST`/`SYSTEMS-MAP` = **zero** in the kit (the "cross-system" hits are all ports); `CANON-VERTICAL-BOUNDARY` is L2 + the wrong axis (core-vs-vertical *within* a product); `SCOPE-DISCIPLINE` (L1, sealed) is the **same family** (orient + respect boundaries before acting) but the **intra-repo** axis → the practice is its **outer ring** (system → module → file). Folded in (no new spine): new **§2.1** (confirm the system; ground-first — learn how it runs, don't from-scratch-build on your own baseline; first-failed-fix → re-evaluate the approach not the depth; maintain a systems-map with **pointers to each system's governing canons**) + a `SYSTEM` field on the Scope Card (§2) + step 0 in the pre-work gate (§7) + rule 0 in the summary (§10). Agnostic (the concrete map = L3); **ViTo's root rules SYSTEMS-MAP + GROUND-FIRST = the L3 binding** (lifted from there). Build-on-pain: a portal architecture oscillated hours despite VERTICAL-BOUNDARY + topology (06-13) already resolving it. | dev-kit architect (ViTo consumer proposal) | Principal Architect | chat ("GO") | this PR; SCOPE-DISCIPLINE §2.1 SEALED |
| D-036 | 2026-06-19 | **FOLD-IN: coder-launch contract + headless design-gate + identity allow-list** (consumer finding — ViTo, first real launch-surface, build-on-pain). 4 parts, advisor-shaped before seal (Marcelo: "como tú recomiendas"): **(1)** `RUNBOOK §3 step 4` — the launch CONTRACT: **prompt fed via stdin** (multi-KB overflows arg length) + **headless + bypass with the deny-guard still biting**; plus an agnostic **2-class runtime frame** (Class 1 API-compatible → env-swap, same launcher; Class 2 own-CLI → own binary/flags, same contract) — the concrete runtime matrix (vendor names, env/flags, the `--yolo`-turns-on-sandbox gotcha) stays **L3** (consumer launch dir), not the neutral runbook. **(2)** `RUNBOOK §7` + a one-clause note to `CODER-ORCHESTRATION §8` — under headless there is **no live pause**, so the boundary design-gate is realized as a **two-phase plan-then-go launch**; refinement applied: **phase-1 plan must be a durable reviewable artifact** (committed plan / draft-PR-with-only-the-plan), never printed stdout (that is not a gate); invariant preserved — plan approved *before* sensitive code, not only the draft PR *after*. **(3A)** identity gate stays an **ALLOW-LIST** (positively confirm the bot); the deny-list variant the finding proposed is **rejected and documented as discouraged** — a denylist on a security gate is the `AUDIT-PROTOCOL §8.1` drift hole (an unenumerated identity passes as the bot) for marginal friction gain. **(3B)** `ADOPT #46` — cite the dispatch rule as a **terse lean-root pointer** (a verbose cite trips the root-budget smoke, `CROSS-AGENT-CONTEXT-LAYERING §6`). One PR (big-PR; CI-predictable). | dev-kit architect (ViTo consumer finding) | Principal Architect | chat ("Aceptado.. como tú recomiendas") | this PR; §8 clause SEALED + RUNBOOK/ADOPT folded |
| D-037 | 2026-06-19 | **AMEND `SCOPE-DISCIPLINE` §2.1 (item 5): a destructive command needs a ground-check, not a baseline assumption** (build-on-pain, forensic-driven). A ViTo agent ran `rm -rf packages work` in the **full monorepo's main worktree** while **believing it was a ~50-file scaffold** (it verified expecting `git ls-files = 56`) → **~1,400 tracked files deleted** (`packages/` + `apps/admin` + `apps/collab-server`); recovered only because committed (cero pérdida). Forensic proof: transcript `63df50c8` line 997 (`rm -rf packages work`) + cwd = ViTo main. Root cause = a **GROUND-FIRST failure on a destructive op** — the §2.1 install/build framing didn't explicitly cover `rm`/delete. Fix: §2.1 item 5 — before any destructive command (`rm -rf` / recursive delete / `reset --hard` / mass removal), **confirm the repo identity** (file count / sentinel / remote); a raw `rm` bypasses every git hook/gate → the agent's pre-destruction check is the **only** guard (a mechanical OS-level blocker = over-engineering + fragile, deliberately NOT built). + summary rule 0 updated. Agnostic (no product named). | dev-kit architect (ViTo forensic) | Principal Architect | chat ("dale si lo consideras que sirva") | this PR; §2.1 item 5 SEALED |
| D-038 | 2026-06-19 | **DOC: kit-refresh trigger — level-3 (SessionStart hook) auto option + the propagation limit** (on-ramp note). `USING-THE-KIT` Refresh trigger now records the **level-3** delivery: wire a SessionStart hook (`CROSS-AGENT-CONTEXT-LAYERING` §9 wiring levels) running `devkit-upgrade --dry-run` so **new** sessions auto-surface the canon-delta — no human memory needed. **Honest limit documented:** it only helps NEW sessions; a **mid-session agent's loaded context cannot be refreshed from outside** (one agent refreshing never updates another live agent's context); only the **shared kit pull + committed wiring** propagate across agents. Answers a consumer question ("if one agent refreshes, do the others adopt?"). No new mechanism (hook is per-harness L3, optional; manual trigger is the floor). | dev-kit architect | Principal Architect | chat ("anotalo") | this PR; USING-THE-KIT level-3 note |
| D-039 | 2026-06-19 | **FOLD-IN: `REVIEW-READINESS-PROTOCOL` §3 — local build is a true signal (compile/flag/determinism), NOT for resource limits** (consumer/advisor proposal — Opus; fold-in, no new spine). Expanded the **Local/workspace** row to include the **real build/compile + flag/tooling compatibility + deterministic build failures** (run the actual build locally, incl. experimental flags, *before* a CI cycle) and the **Gate/CI** row to own **resource limits (OOM/timeout/runner-cap)** that local can't fake. Added the **resource-fidelity caveat + save-CI binding**: local-first for the *blocking* question (compiles? flag works? reproduces?) kills the push→red→repush loop and saves paid CI minutes; Local is **not** faithful for resource failures unless constrained to the runner envelope (cap RAM/workers/cold-cache) — else Gate/CI is the true signal; if local repro needs setup beyond the question, **report + pivot**. Reinforces the existing *consumer-over-unit / build-the-affected-dependents* corollary (D-026). Agnostic (the L3 binding — e.g. the consuming repo's dev-server rule — stays L3). Aligned with the sealed *governance-must-accelerate / save-CI* value. Done as a clear forwarded process-protocol improvement (not sealed canon → delegated-with-record). | dev-kit architect (Opus consumer proposal) | delegated | chat (forwarded proposal) | this PR; §3 expanded |
| D-040 | 2026-06-19 | **(A) SEAL `CANON-CODER-ORCHESTRATION-001` §9.1 — the Fan-Out Gate + (B) DEFER its automation, tracked** (consumer elevation — Campus/Fable). §9 described the wave (fan-out vs sequential) but didn't **force** evaluating it → depended on agent memory → failed live (the human had to push with 2 ready independent specs — the exact "don't make me push for fan-out" pain). **(A) §9.1 Fan-Out Gate (SEALED):** a mandatory dispatch-point question — *"≥2 units READY and INDEPENDENT? → fan out by default; if not, name the one blocker"* — + 6 checkable conditions (ready · independent · no-order · integrated-base · owner · human-cost); default to the wave, present-the-choice (§3.1), sequencing is the declared exception. **(B) DEFER the `fan-out-readiness` instrument** (machine-surfaces candidates; units declare a structured `surface`; prose-mining is fragile per the pilot) — **build-on-pain:** the failure was behavioral, the manual gate fixes it; automation adds a structured-surface adoption cost. **Anti-baúl (Marcelo: "documentado + accionable cuando madure, no al baúl de los recuerdos"):** new `doc/DEFERRED-INSTRUMENTS.md` backlog records it with a **falsifiable trigger** (build when §9.1 demonstrably fails), and §9.1 carries that trigger at the point of use. No new canon (fold to §9). Canonizes Marcelo's own fan-out-by-default rule. | dev-kit architect (Campus/Fable elevation) | Principal Architect | chat ("sigo tus recomendaciones … que queden documentadas y accionables … que no se quede en el baúl") | this PR; §9.1 SEALED + DEFERRED-INSTRUMENTS created |
| D-041 | 2026-06-19 | **AMEND `CANON-BRANCH-WORKTREE-LIFECYCLE` §5.2 — the worktree's unique id IS the full work-item slug, never a truncation** (consumer finding — Campus; 2nd occurrence, the durable cure). D-021 said "keyed by a unique id" but didn't fix WHAT the id is → the L3 launcher kept keying on a **numeric/truncated prefix** → a number isn't unique across concurrent fronts → two work-items resolve to the **same** isolation path, the 2nd silently runs the wrong front (wrong spec, wrong-branch push). Evidence: two fronts both numbered `045` collided on one dir (1st was `037b`→`037`, patched by changing the truncation but **keeping a truncation as the key let the class survive**). Fix: id = **full slug** (not a prefix) + **reuse guard** (isolation unit exists on a *different* branch/work-item → **ABORT**, broader than the stale-leftover case) + numbering hygiene (next-id over all id-bearing artifacts, re-verify at dispatch). **Anti-over-engineering:** the proposed separate "path-unique vs in-flight" check is **not** added — full-slug identity makes path-collision impossible by construction; the abort guard is the backstop. Agnostic (the `Split`/launcher diff is the L3 worked-example in the consumer's adapter). Accelerates: kills wrong-front runs → fewer junk PRs on paid CI + less cleanup. | dev-kit architect (Campus finding) | Principal Architect | chat ("GO") | this PR; §5.2 amended |
| D-043 | 2026-06-19 | **SHARPEN `BRANCH-WORKTREE-LIFECYCLE` §5.2 numbering line — sequential numbers are NOT concurrency-safe** (consumer update — Campus; corrects D-041's over-optimism). The "re-verify at dispatch — concurrent agents claim numbers" line implied re-verification keeps numbers unique; it doesn't (two parallel arqs read the same max → both pick the next; live: 044/045/046 collisions). Fix: re-verify **narrows but does not close** the concurrent-claim race; per the slug-identity rule (D-041) **the number is a non-authoritative label** → its collision is **cosmetic** (slug-keyed worktrees/branches don't collide), not fatal; a unique-number **registry is an L3 OPTION** (human-legibility nicety, *not* the cure — the slug already prevents the fatal collision). **Resolves the consumer's FINDING-PROCESS agnostically:** don't build a number registry for correctness; the slug-identity already saves you. (The companion FINDING-INFRA — `launch-coder.ps1` keying on the numeric tag — is **already covered by D-041**; its fix is ViTo applying D-041 to the launcher, L3.) | dev-kit architect (Campus update) | Principal Architect | chat ("GO") | this PR; §5.2 numbering line sharpened |
| D-042 | 2026-06-19 | **NEW `ADR-20260619` — shared runtime package distribution = an org-scoped package registry** (canonize a sealed decision, agnostic). Gap: `ADR-20260524` covers kit→consumer (docs-by-ref / runnable-by-copy+parity) and copy-parity covers small dep-free files, but **nothing covered a first-party runtime package, versioned + with deps, shared by ≥2 repos**. Decision (agnostic): such a package is **published to an org-scoped registry and consumed as a versioned dependency** (scoped-registry config + env token + SemVer), **never copied**. The cut: **deps+versioned → registry · dep-free small canonical file → copy-parity · docs → reference.** **Form:** an **ADR** (sibling of `ADR-20260524`), not a new canon — matches the kit's precedent (cross-repo distribution mechanisms are ADRs) + lighter (no catalog piece). **Agnostic technique:** capability-not-vendor + concept-then-"e.g." + explicit L1/L3 split + fire-test; all concretes (registry/org/scope/token/config) are L3, the ViTo binding (GitHub Packages · `vibethink-edu` · `@vibethink` · `GITHUB_TOKEN` · `@vibethink/rich-editor`) lives only in a labeled worked-example footnote. + a boundary pointer in ADOPT piece #31. | dev-kit architect (sealed decision, ViTo) | Principal Architect | chat ("GO y cómo la vuelves agnóstica?") | this PR; ADR-20260619 created |
| D-044 | 2026-06-19 | **FIX `external-tools.lock.json` — the graphify pin's `package` is `graphifyy`, not `graphify`** (revert D-024 regression; consumer-caught empirically). D-024 had changed `package: "graphifyy"`→`"graphify"` assuming the double-`y` was a typo — **it isn't**: the PyPI **package** is `graphifyy` (double-y), the installed **CLI binary** is `graphify` (single-y); they are not the same name. Effect: `pip install graphify` finds no distribution → any consumer following the kit's refresh suggestion silently fails. **Verified empirically:** `pip index versions graphify` → none · `pip show graphifyy` → installed provider · `graphify --version` → 0.8.39. Fix = lock `package`→`graphifyy` (the **only** wrong place — `EXTERNAL-TOOLS.md`, `check-tools.sh`, the install scripts already say `graphifyy`); kept `name: graphify` / `cli: graphify` / `pin: 0.8.39` unchanged. + a `DEFERRED-INSTRUMENTS` row (external-tools pin integrity check) so the lock↔doc drift class is tracked. **Delegated-with-record:** my own regression, a 1-field direct-path fix (per the direct-execution-for-short-fixes feedback) — no separate authority gate. | dev-kit architect (own regression; consumer FYI, ViTo) | dev-kit architect (delegated, recorded) | chat ("validar esto FYI … graphify en vez de graphifyy") | this PR; lock `package`→`graphifyy` + DEFERRED row |
| D-045 | 2026-06-20 | **SEAL PR #184 — `RUNBOOK-LAUNCH-CODERS` §7: a two-phase re-launch must not strand the operator** (consumer elevation — Campus, transporte-arq; merge of another agent's doc PR). The §8 two-phase design-gate (D-036) defined the *what* but the RUNBOOK didn't say *how to hand back to the operator* between PLAN and IMPLEMENT → a real run stranded the operator: the L3 launcher did a **bare `cd`** into the worktree and never returned, so the launch script was unreachable for phase 2 ("do I close the bot and run it again?" / "the launch-coder is no longer there"; the plan-phase coder had already exited cleanly — the confusion was pure cwd + a missing next-step hint). Fix (heritable, agnostic): a two-phase launcher MUST **(a)** return to the invocation dir (push/pop, not a bare `cd`) and **(b)** on the PLAN phase **print the exact phase-2 command** + the mental model — phases 1 and 2 are **two runs of the same launcher sharing the *worktree*** (the worktree, not a live process, carries the approved plan across the GO). **Build-on-pain:** codifies the orchestrator L3's known-good push/pop as the norm after it bit a 2nd consumer; RUNBOOK doc only (no canon/code change). Fire-test passes (says "the launcher/worktree/operator", no L3 names). | transporte-arq (Campus elevation) | dev-kit architect (review + seal) | comm + PR #184 | merged #184; RUNBOOK §7 amended |
| D-046 | 2026-06-20 | **SEAL agent identity = `<product>-<role>`, decoupled from the runtime brand** (Marcelo SUPRA seal; proposal from vito-architect, origin/main ViTo). The naming canon was agnostic in *form* but still **sanctioned vendor/runtime brands as the branch-author token** (§9: "agent/vendor tokens are legitimate … in the branch-author roster") — coupling governance provenance to a tool choice. Seal: an agent's **governance identity** (branch author, addressing, comms, closeout) is **`<product>-<role>`** (house-named, agnostic); the tool that runs it is a **runtime**, catalogued in `AI_AGENT_COMPATIBILITY`, never the identity. **Three canon touches:** (1) `NAMING-CONVENTIONS-001` new **§3.1** (identity vs runtime; going-forward only, historical names immutable; adoption-per-role is L3) + §9 two "never" lines (identity in the roster, never a brand); (2) `MULTI-AGENT-ORCHESTRATION` addressing — the `codex`/`codex-rev` literal examples → agnostic `<product>-coder` placeholders + an identity-not-runtime note; (3) `AI_AGENT_COMPATIBILITY` — a clarify that it lists **runtimes**, identity orthogonal (a `<product>-coder` may run on any tool listed). **Agnostic form (my architect call):** sealed the pattern **role-general** (`<product>-<role>`), not the instance `<product>-coder` — a canon states the pattern; this covers coder/architect/reviewer **by construction**, so Marcelo's open question (*extend to `<product>-architect`/`<product>-reviewer`?*) is answered at SUPRA by construction and the *adoption per role/product* is an **L3 going-forward** call (ViTo binds Rule #15 `codex/`→`vito-coder/` size-neutral; Campus → `campus-coder`). Fire-test: neutral-core bodies use `<product>-<role>` placeholders only; brand names live solely in the runtime matrix. | Marcelo (SUPRA seal) | Marcelo / dev-kit architect (agnostic form) | comm "Propuesta de naming SUPRA (sello de Marcelo)" + PROPOSAL-OPUS-AGENT-IDENTITY-RENAME-…-20260620 | this PR; 3 canons amended |
| D-047 | 2026-06-20 | **NEW `CANON-SYSTEM-MIGRATION-DISCIPLINE-001` — moving a legacy system → a new one is a governed discipline** (consumer elevation — Campus; Marcelo approved "GO"). A legacy→new data+schema move had no agnostic norm → the silent-gap failure (*"did a field get left unmigrated? backfilled from the wrong rule? applied out of order?"*) discovered after cutover, with the legacy already gone. Seals **4 primitives** (extracted agnostic from the Campus OASYS→new move, bound to the recognized standard expand-contract · schema-migration+backfill · cutover-runbook · ETL data-provenance — formalizes, invents no house term): (1) **provenance tagging** `DERIVED`/`GREENFIELD`/`SEED` (gate: nothing without provenance); (2) **as-built MIGRATION-PLAYBOOK** (1 doc/destination · 1 row/change · same PR); (3) **cutover-readiness** `written→verify(pos+NEG)→apply` + explicit dependency order; (4) **governed apply+record** (hydrated admin cred, never app-runtime/in-repo → surgical per-migration applier tx+self-test → apply-ledger). **Location = my architect call (delegated): Option A (new canon), NOT B (fold into `CANON-DB-SCHEMA-LIFECYCLE`)** — B unviable, that canon does not exist in the SUPRA (it is L3 ViTo); A also gives the pattern its own reusable identity. **Spine-coverage:** primitives 1-3 are net-new; **primitive 4 cross-refs `CANON-DATA-CHANGE-AUDIT-001`** (its §10.2 already excludes "migration ledger" from the runtime audit-trail → explicit §6 boundary, no duplication) + cross-refs DATA-LEGAL-COMPLIANCE (consent/retention of migrated datum) + DB-SECURITY-BASELINE (RLS on created tables). + ADOPT piece **#47**. Campus = reference impl L3. Fire-test passes (no product in normative body; Campus only in dated note). | Marcelo (canon approval "GO") | Principal Architect (Marcelo) | comm "Propuesta de Campus-arq … migración" + PR #195 (Campus) + chat "GO" | this PR; canon + piece #47 created |

<!--
  Distinct from the audit-finding disposition ledger (CANON-AUDIT-PROTOCOL §4):
  that ledger carries FINDINGS to closure; this register carries AUTHORITY APPROVALS.
  Add a row the moment an authority-class "go" is granted — a reconstructed approval
  is a recollection, not a record.
-->
