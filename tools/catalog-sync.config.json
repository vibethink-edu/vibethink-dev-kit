{
  "$schema": "VIBETHINK_CATALOG_SYNC_V1",
  "_comment": "Producer-side gate config (kit-only; consumers never run this). Check 1: every spine matching spinePattern in knowledgeDirs must be cited by the catalog or carry an exemption WITH a reason. Check 2: every spine (+ alsoStatusCheck files) declares a **Status:** line starting with a controlled token — the mechanical end of mixed status vocabulary (new spines conform on arrival; sealed headers are not cosmetically rewritten).",
  "catalogFile": "setup/ADOPT-DEV-KIT.md",
  "contractFile": "setup/INHERITANCE-CONTRACT.md",
  "knowledgeDirs": ["knowledge/ai-agents", "knowledge/methodology", "knowledge/architecture"],
  "spinePattern": "^(CANON-.*|VT-METHOD)\\.md$",
  "exempt": [
    {
      "file": "knowledge/architecture/CANON-VERTICAL-BOUNDARY-001.md",
      "reason": "DRAFT pending Principal Architect seal (added by Campus 2026-06-12, PR #68; also an open layer question — L2-house naming a product). Not yet an adoptable piece: its catalog section is added when Marcelo seals it (and the exemption removed then). Until then it must not block the catalog of adoptable pieces — but it is not silently absent either."
    },
    {
      "file": "knowledge/architecture/CANON-VERTICAL-UI-INHERITANCE-001.md",
      "reason": "DRAFT pending Principal Architect seal (added 2026-06-13). Proposed registration as ADOPT piece #32 + inheritance-status row is deferred to the seal (governance numbering is the architect's call). Until sealed it must not block the catalog of adoptable pieces — but it is not silently absent either."
    },
    {
      "file": "knowledge/ai-agents/CANON-OBSERVABILITY-CLOSED-LOOP-001.md",
      "reason": "DRAFT — INTENTION handed from a consuming product (ViTo, HANDOFF 2026-06-17). Declared as an agnostic working agreement, NOT law. It is formalized by ViTo (front observability-closed-loop, P0->P1->P2); only on validated formalization does the Principal Architect promote it to a sealed canon and register it as an adoptable piece (exemption removed then). Until then it must not block the catalog — but it is not silently absent either."
    },
    {
      "file": "knowledge/methodology/CANON-DOCUMENTATION-ARTIFACT-STANDARDS-001.md",
      "reason": "PROPOSED 2026-06-18 (WorkBench-architect handoff; Marcelo principle + GO). Binds each doc artifact to its recognized external standard (MADR/RFC/PRR/Diátaxis/…) + ships templates. NOT yet sealed: one open decision (the work-journal, §5) must be resolved by the Principal Architect first. Registered as an adoptable piece + exemption removed AT SEAL (per kit pattern + handoff item 5: consumers re-bind after seal — registering the piece before seal would make consumer adoption-gates require a row for an unsealed canon). Until then it must not block the catalog — but it is not silently absent either."
    }
  ],
  "statusTokens": [
    "SEALED",
    "approved",
    "CANON",
    "DRAFT",
    "PROPOSED",
    "process protocol",
    "BASELINE"
  ],
  "alsoStatusCheck": [
    "knowledge/ai-agents/REVIEW-CALL-CHECKLIST.md",
    "knowledge/ai-agents/REVIEW-READINESS-PROTOCOL.md"
  ]
}
