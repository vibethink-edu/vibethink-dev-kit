{
  "_about": "Declares this repo's cross-agent context layout for tools/check-agent-context.mjs (canon §6/§7). Each fork edits this to point at its own root rules file and adapters.",
  "rootRulesFile": "knowledge/ai-agents/AGENTS_UNIVERSAL.md",
  "canonFile": "knowledge/ai-agents/CANON-CROSS-AGENT-CONTEXT-LAYERING.md",
  "agentsDir": "knowledge/ai-agents",
  "agentBudgets": {
    "codex": 32768,
    "copilot": 32768,
    "windsurf": 32768,
    "gemini": 32768,
    "claude": 1000000
  },
  "adapters": [
    { "agent": "claude", "file": "knowledge/ai-agents/CLAUDE.md" },
    { "agent": "codex", "file": "knowledge/ai-agents/CODEX.md" },
    { "agent": "copilot", "file": "knowledge/ai-agents/COPILOT_INSTRUCTIONS.md" },
    { "agent": "windsurf", "file": "knowledge/ai-agents/WINDSURFRULES.md" }
  ],
  "maxAdapterBytes": 6144,
  "nonAdapterAgentFiles": ["AI_AGENT_COMPATIBILITY.md"],
  "agentFilePattern": "^(CLAUDE|CODEX|COPILOT_INSTRUCTIONS|WINDSURFRULES|GEMINI|AGENTS)\\.md$",
  "requiredAnchorsInRoot": ["CANON-CROSS-AGENT-CONTEXT-LAYERING"],
  "agentConfigs": [],
  "_agentConfigsNote": "Empty on purpose (criterion #8): the root authority is under the strictest budget (32 KiB) natively, so no per-agent override is required for this kit. A fork whose root exceeds a strict agent's budget MUST declare its tracked override file here (e.g. .codex/config.toml).",
  "ruleNumberPattern": null,
  "_ruleNumberPatternNote": "null = this root uses LEVEL sections, not a numbered rule list, so the sequence check is skipped. A repo with numbered rules sets this to e.g. \"^> ?(\\\\d+)\\\\.\" to enable the gap/duplicate check.",
  "secretPatterns": [
    "ghp_[A-Za-z0-9]{36}",
    "github_pat_[A-Za-z0-9_]{60,}",
    "gho_[A-Za-z0-9]{36}",
    "(?:sk|rk)-[A-Za-z0-9]{32,}",
    "xox[baprs]-[A-Za-z0-9-]{10,}",
    "AKIA[0-9A-Z]{16}"
  ],
  "secretScanExtensions": [
    ".md",
    ".mdx",
    ".json",
    ".js",
    ".mjs",
    ".cjs",
    ".ts",
    ".tsx",
    ".toml",
    ".yml",
    ".yaml",
    ".ps1",
    ".sh",
    ".env",
    ".txt",
    ".cursorrules",
    ".template"
  ],
  "secretExcludeContains": ["tools/agent-context.config.json", "tools/check-agent-context.mjs"]
}
